use crate::cap3::data_types::data_types;
use crate::cap3::shadowing::shadowing;
use crate::cap3::var_and_mut::var_and_mut;
use crate::cap3::statements::statements;


pub fn cap3() {
    data_types();
    shadowing();
    var_and_mut();
    statements();
}